#include "shape.h"

Layer _planMessage(Layer pLayer, u_int colorScheme, char x, char y);

Layer _sunMessage(Layer sLayer, u_int colorScheme, char x, char y);

void drawStars(char rand);

void drawSpace();
